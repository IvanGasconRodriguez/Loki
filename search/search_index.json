{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Loki AI Agent","text":"<p>Loki es un agente de inteligencia artificial orientado a CRM que interpreta mensajes en lenguaje natural y ejecuta acciones sobre oportunidades.</p>"},{"location":"#que-hace","title":"\ud83d\ude80 Qu\u00e9 hace","text":"<ul> <li>Interpreta intenci\u00f3n del usuario usando OpenAI</li> <li>Ejecuta acciones sobre oportunidades (CRM)</li> <li>Genera res\u00famenes autom\u00e1ticos</li> <li>Prioriza leads y detecta bloqueos</li> </ul>"},{"location":"#capacidades","title":"\ud83e\udde0 Capacidades","text":"<ul> <li>move_opportunity</li> <li>add_tag</li> <li>create_task</li> <li>resumen autom\u00e1tico</li> <li>priorizaci\u00f3n de leads</li> </ul>"},{"location":"#stack","title":"\u2699\ufe0f Stack","text":"<ul> <li>FastAPI</li> <li>OpenAI (LLM)</li> <li>HTTPX (API externa CRM)</li> </ul>"},{"location":"architecture/","title":"Arquitectura","text":"<p>El sistema sigue un flujo basado en agentes:</p> <p>Usuario \u2192 API \u2192 LLM \u2192 L\u00f3gica \u2192 CRM</p>"},{"location":"architecture/#componentes","title":"Componentes","text":""},{"location":"architecture/#1-api-mainpy","title":"1. API (main.py)","text":"<p>Expone endpoint <code>/loki/chat</code></p>"},{"location":"architecture/#2-llm-servicesllmpy","title":"2. LLM (services/llm.py)","text":"<p>Genera respuestas explicativas</p>"},{"location":"architecture/#3-intencion-logicintentionpy","title":"3. Intenci\u00f3n (logic/intention.py)","text":"<p>Convierte texto en JSON estructurado</p>"},{"location":"architecture/#4-reglas-logicrulespy","title":"4. Reglas (logic/rules.py)","text":"<p>Analiza estado del CRM</p>"},{"location":"architecture/#5-integracion-crm-servicesghlpy","title":"5. Integraci\u00f3n CRM (services/ghl.py)","text":"<p>Comunicaci\u00f3n con API externa</p>"},{"location":"architecture/#flujo-completo","title":"Flujo completo","text":"<ol> <li>Usuario env\u00eda mensaje</li> <li>Se obtienen oportunidades del CRM</li> <li>Se normalizan datos</li> <li>LLM interpreta intenci\u00f3n</li> <li>Se ejecuta l\u00f3gica seg\u00fan acci\u00f3n</li> <li>Se devuelve respuesta estructurada</li> </ol>"},{"location":"configuration/","title":"Configuraci\u00f3n","text":""},{"location":"configuration/#variables-de-entorno","title":"Variables de entorno","text":"<p>```env OPENAI_API_KEY=... GHL_ACCESS_TOKEN=... GHL_LOCATION_ID=...</p> <p>Carga variables desde .env</p>"},{"location":"limitaciones/","title":"Limitaciones","text":"<ul> <li>Dependencia de OpenAI API</li> <li>Posibles errores en interpretaci\u00f3n</li> <li>Datos incompletos del CRM</li> <li>Latencia de red</li> </ul>"},{"location":"logic/","title":"L\u00f3gica","text":""},{"location":"logic/#modulo-de-logica","title":"M\u00f3dulo de l\u00f3gica","text":""},{"location":"logic/#intencion","title":"Intenci\u00f3n","text":""},{"location":"logic/#logic.intention.interpretar_intencion","title":"<code>interpretar_intencion(message)</code>  <code>async</code>","text":"<p>Interpreta la intenci\u00f3n del usuario a partir de un mensaje en lenguaje natural utilizando un modelo de OpenAI.</p> <p>El modelo devuelve una respuesta en formato JSON con una acci\u00f3n espec\u00edfica relacionada con operaciones en el CRM.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Texto enviado por el usuario.</p> required <p>Returns:</p> Name Type Description <code>dict</code> <code>dict</code> <p>Diccionario con la intenci\u00f3n detectada. Ejemplo: {     \"action\": \"move_opportunity\",     \"opportunity_name\": \"Juan\",     \"target_stage\": \"Negociaci\u00f3n\",     \"tag\": \"...\",     \"task_description\": \"...\" }</p> <code>dict</code> <p>En caso de error o si no se detecta intenci\u00f3n clara: {\"action\": \"none\"}</p> Source code in <code>logic\\intention.py</code> <pre><code>async def interpretar_intencion(message: str) -&gt; dict:\n    \"\"\"\n    Interpreta la intenci\u00f3n del usuario a partir de un mensaje en lenguaje natural\n    utilizando un modelo de OpenAI.\n\n    El modelo devuelve una respuesta en formato JSON con una acci\u00f3n espec\u00edfica\n    relacionada con operaciones en el CRM.\n\n    Args:\n        message (str): Texto enviado por el usuario.\n\n    Returns:\n        dict: Diccionario con la intenci\u00f3n detectada. Ejemplo:\n            {\n                \"action\": \"move_opportunity\",\n                \"opportunity_name\": \"Juan\",\n                \"target_stage\": \"Negociaci\u00f3n\",\n                \"tag\": \"...\",\n                \"task_description\": \"...\"\n            }\n\n        En caso de error o si no se detecta intenci\u00f3n clara:\n            {\"action\": \"none\"}\n    \"\"\"\n    response = await client.chat.completions.create(\n        model=\"gpt-4o-mini\",\n        temperature=0,\n        messages=[\n            {\"role\": \"system\", \"content\": SYSTEM_PROMPT},\n            {\"role\": \"user\", \"content\": message}\n        ]\n    )\n\n    content = response.choices[0].message.content\n\n    try:\n        return json.loads(content)\n    except:\n        return {\"action\": \"none\"}\n</code></pre>"},{"location":"logic/#resolver","title":"Resolver","text":""},{"location":"logic/#logic.resolver.find_opportunity_by_name","title":"<code>find_opportunity_by_name(state, name)</code>","text":"<p>Busca una oportunidad dentro del estado del CRM por coincidencia de nombre.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>list</code> <p>Lista de oportunidades normalizadas.</p> required <code>name</code> <code>str</code> <p>Nombre (o parte del nombre) de la oportunidad.</p> required <p>Returns:</p> Type Description <code>dict | None</code> <p>dict | None: La oportunidad encontrada o None si no existe coincidencia.</p> Source code in <code>logic\\resolver.py</code> <pre><code>def find_opportunity_by_name(state: list, name: str) -&gt; dict | None:\n    \"\"\"\n    Busca una oportunidad dentro del estado del CRM por coincidencia de nombre.\n\n    Args:\n        state (list): Lista de oportunidades normalizadas.\n        name (str): Nombre (o parte del nombre) de la oportunidad.\n\n    Returns:\n        dict | None: La oportunidad encontrada o None si no existe coincidencia.\n    \"\"\"\n    if not name:\n        return None\n\n    name = name.lower()\n\n    for o in state:\n        if name in o[\"name\"].lower():\n            return o\n\n    return None\n</code></pre>"},{"location":"logic/#logic.resolver.resolve_stage_id","title":"<code>resolve_stage_id(stage_name)</code>","text":"<p>Convierte un nombre de etapa a su ID correspondiente dentro del pipeline.</p> <p>La b\u00fasqueda se realiza de forma flexible (case-insensitive y parcial).</p> <p>Parameters:</p> Name Type Description Default <code>stage_name</code> <code>str</code> <p>Nombre de la etapa (ej. \"Negociaci\u00f3n\").</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>str | None: ID de la etapa o None si no se encuentra.</p> Source code in <code>logic\\resolver.py</code> <pre><code>def resolve_stage_id(stage_name: str) -&gt; str | None:\n    \"\"\"\n    Convierte un nombre de etapa a su ID correspondiente dentro del pipeline.\n\n    La b\u00fasqueda se realiza de forma flexible (case-insensitive y parcial).\n\n    Args:\n        stage_name (str): Nombre de la etapa (ej. \"Negociaci\u00f3n\").\n\n    Returns:\n        str | None: ID de la etapa o None si no se encuentra.\n    \"\"\"\n    if not stage_name:\n        return None\n\n    for key in STAGE_MAP:\n        if stage_name.lower() in key.lower():\n            return STAGE_MAP[key]\n\n    return None\n</code></pre>"},{"location":"logic/#reglas","title":"Reglas","text":""},{"location":"logic/#logic.rules.discovery_bloqueada","title":"<code>discovery_bloqueada(state, hours=24)</code>","text":"<p>Identifica oportunidades en etapa de discovery que no han sido actualizadas.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>list</code> <p>Lista de oportunidades.</p> required <code>hours</code> <code>int</code> <p>N\u00famero de horas sin actualizaci\u00f3n.</p> <code>24</code> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>Oportunidades bloqueadas.</p> Source code in <code>logic\\rules.py</code> <pre><code>def discovery_bloqueada(state: list, hours: int = 24) -&gt; list:\n    \"\"\"\n    Identifica oportunidades en etapa de discovery que no han sido actualizadas.\n\n    Args:\n        state (list): Lista de oportunidades.\n        hours (int): N\u00famero de horas sin actualizaci\u00f3n.\n\n    Returns:\n        list: Oportunidades bloqueadas.\n    \"\"\"\n    now = datetime.now(timezone.utc)\n    result = []\n\n    for o in state:\n        if o[\"stage_id\"] != DISCOVERY_AGENDADA_STAGE_ID:\n            continue\n\n        updated = parse_iso_date(o[\"updated_at\"])\n        if not updated:\n            continue\n\n        if now - updated &gt; timedelta(hours=hours):\n            result.append(o)\n\n    return result\n</code></pre>"},{"location":"logic/#logic.rules.generar_resumen_diario","title":"<code>generar_resumen_diario(state)</code>","text":"<p>Genera un resumen del estado del CRM incluyendo m\u00e9tricas clave.</p> <p>El resumen incluye: - Leads sin contacto - Discoveries bloqueadas - Propuestas sin respuesta</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>list</code> <p>Lista de oportunidades normalizadas.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <ul> <li>str: Texto del resumen</li> <li>list: Leads sin contacto</li> <li>list: Discoveries bloqueadas</li> <li>list: Propuestas sin respuesta</li> </ul> Source code in <code>logic\\rules.py</code> <pre><code>def generar_resumen_diario(state: list):\n    \"\"\"\n    Genera un resumen del estado del CRM incluyendo m\u00e9tricas clave.\n\n    El resumen incluye:\n    - Leads sin contacto\n    - Discoveries bloqueadas\n    - Propuestas sin respuesta\n\n    Args:\n        state (list): Lista de oportunidades normalizadas.\n\n    Returns:\n        tuple:\n            - str: Texto del resumen\n            - list: Leads sin contacto\n            - list: Discoveries bloqueadas\n            - list: Propuestas sin respuesta\n    \"\"\"\n    leads = leads_sin_contacto(state)\n    discoveries = discovery_bloqueada(state)\n    propuestas = propuestas_sin_respuesta(state)\n\n    resumen = \"\ud83d\udcca Resumen diario CRM:\\n\\n\"\n\n    resumen += f\"\u2022 Leads sin primer contacto: {len(leads)}\\n\"\n    resumen += f\"\u2022 Discoveries pendientes de mover: {len(discoveries)}\\n\"\n    resumen += f\"\u2022 Propuestas sin seguimiento: {len(propuestas)}\\n\"\n\n    return resumen, leads, discoveries, propuestas\n</code></pre>"},{"location":"logic/#logic.rules.leads_sin_contacto","title":"<code>leads_sin_contacto(state, minutes=60)</code>","text":"<p>Detecta leads en etapa inicial que no han recibido contacto reciente.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>list</code> <p>Lista de oportunidades normalizadas.</p> required <code>minutes</code> <code>int</code> <p>Tiempo l\u00edmite en minutos para considerar inactividad.</p> <code>60</code> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>Lista de leads que requieren contacto inmediato.</p> Source code in <code>logic\\rules.py</code> <pre><code>def leads_sin_contacto(state: list, minutes: int = 60) -&gt; list:\n    \"\"\"\n    Detecta leads en etapa inicial que no han recibido contacto reciente.\n\n    Args:\n        state (list): Lista de oportunidades normalizadas.\n        minutes (int): Tiempo l\u00edmite en minutos para considerar inactividad.\n\n    Returns:\n        list: Lista de leads que requieren contacto inmediato.\n    \"\"\"\n    now = datetime.now(timezone.utc)\n    result = []\n\n    for o in state:\n        # Solo Lead Entrante\n        if o[\"stage\"] != LEAD_ENTRANTE_STAGE_ID:\n            continue\n\n        # Excluir si ya fue contactado\n        if TAG_CONTACTO_REALIZADO in o.get(\"tags\", []):\n            continue\n\n        # SLA tiempo\n        created = parse_iso_date(o[\"created_at\"])\n        if not created:\n            continue\n\n        if now - created &gt; timedelta(minutes=minutes):\n            result.append(o)\n\n    return result\n</code></pre>"},{"location":"logic/#logic.rules.normalize_opportunities","title":"<code>normalize_opportunities(opps)</code>","text":"<p>Normaliza la estructura de oportunidades obtenidas del CRM.</p> <p>Convierte los datos en un formato uniforme para facilitar su procesamiento en el agente.</p> <p>Parameters:</p> Name Type Description Default <code>opps</code> <code>list</code> <p>Lista de oportunidades crudas desde la API.</p> required <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>Lista de oportunidades normalizadas con campos est\u00e1ndar.</p> Source code in <code>logic\\rules.py</code> <pre><code>def normalize_opportunities(opps: list) -&gt; list:\n    \"\"\"\n    Normaliza la estructura de oportunidades obtenidas del CRM.\n\n    Convierte los datos en un formato uniforme para facilitar su procesamiento\n    en el agente.\n\n    Args:\n        opps (list): Lista de oportunidades crudas desde la API.\n\n    Returns:\n        list: Lista de oportunidades normalizadas con campos est\u00e1ndar.\n    \"\"\"\n    state = []\n\n    for o in opps:\n        # Usamos .get() para evitar que el programa se detenga si falta alg\u00fan campo\n        # GHL en la b\u00fasqueda b\u00e1sica env\u00eda 'pipelineStageId' y 'pipelineId'\n        state.append(\n            {\n                \"id\": o.get(\"id\"),\n                \"name\": o.get(\"name\", \"Sin nombre\"),\n                # Si no viene el nombre de la etapa, ponemos el ID para que no de error\n                \"stage\": o.get(\"pipelineStageId\", \"N/A\"),\n                \"pipeline\": o.get(\"pipelineId\", \"N/A\"),\n                \"owner\": o.get(\"assignedTo\"),\n                \"created_at\": o.get(\"createdAt\"),\n                \"source\": o.get(\"source\")\n            }\n        )\n\n    return state\n</code></pre>"},{"location":"logic/#logic.rules.parse_iso_date","title":"<code>parse_iso_date(value)</code>","text":"<p>Convierte una cadena de fecha en formato ISO a un objeto datetime.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>Fecha en formato ISO (ej. \"2024-01-01T10:00:00Z\").</p> required <p>Returns:</p> Type Description <p>datetime | None: Objeto datetime o None si el formato es inv\u00e1lido.</p> Source code in <code>logic\\rules.py</code> <pre><code>def parse_iso_date(value: str):\n    \"\"\"\n    Convierte una cadena de fecha en formato ISO a un objeto datetime.\n\n    Args:\n        value (str): Fecha en formato ISO (ej. \"2024-01-01T10:00:00Z\").\n\n    Returns:\n        datetime | None: Objeto datetime o None si el formato es inv\u00e1lido.\n    \"\"\"\n    if isinstance(value, str):\n        return datetime.fromisoformat(value.replace(\"Z\", \"+00:00\"))\n    return None\n</code></pre>"},{"location":"logic/#logic.rules.propuestas_sin_respuesta","title":"<code>propuestas_sin_respuesta(state, days=3)</code>","text":"<p>Detecta oportunidades en etapa de propuesta que no han tenido seguimiento.</p> <p>Parameters:</p> Name Type Description Default <code>state</code> <code>list</code> <p>Lista de oportunidades.</p> required <code>days</code> <code>int</code> <p>D\u00edas sin respuesta.</p> <code>3</code> <p>Returns:</p> Name Type Description <code>list</code> <code>list</code> <p>Oportunidades sin seguimiento.</p> Source code in <code>logic\\rules.py</code> <pre><code>def propuestas_sin_respuesta(state: list, days: int = 3) -&gt; list:\n    \"\"\"\n    Detecta oportunidades en etapa de propuesta que no han tenido seguimiento.\n\n    Args:\n        state (list): Lista de oportunidades.\n        days (int): D\u00edas sin respuesta.\n\n    Returns:\n        list: Oportunidades sin seguimiento.\n    \"\"\"\n    now = datetime.now(timezone.utc)\n    result = []\n\n    for o in state:\n        if o[\"stage_id\"] != PROPUESTA_ENVIADA_STAGE_ID:\n            continue\n\n        updated = parse_iso_date(o[\"updated_at\"])\n        if not updated:\n            continue\n\n        if now - updated &gt; timedelta(days=days):\n            result.append(o)\n\n    return result\n</code></pre>"},{"location":"prompt/","title":"Prompt del sistema","text":"<p>El agente usa un prompt estructurado para forzar salida JSON.</p>"},{"location":"prompt/#reglas","title":"Reglas","text":"<ul> <li>Siempre JSON v\u00e1lido</li> <li>Sin texto extra</li> <li>Acciones definidas</li> </ul>"},{"location":"prompt/#acciones","title":"Acciones","text":"<ul> <li>move_opportunity</li> <li>add_tag</li> <li>create_task</li> <li>none</li> </ul>"},{"location":"requirements/","title":"Requisitos","text":"<p>pip install fastapi uvicorn httpx openai python-dotenv</p>"},{"location":"rules/","title":"Reglas del negocio","text":""},{"location":"rules/#leads-sin-contacto","title":"Leads sin contacto","text":"<p>Detecta leads en etapa inicial sin actividad reciente</p>"},{"location":"rules/#discovery-bloqueada","title":"Discovery bloqueada","text":"<p>Detecta oportunidades sin actualizaci\u00f3n</p>"},{"location":"rules/#propuestas-sin-respuesta","title":"Propuestas sin respuesta","text":"<p>Detecta oportunidades sin seguimiento</p>"},{"location":"services/","title":"Servicios","text":""},{"location":"services/#openai","title":"OpenAI","text":""},{"location":"services/#services.llm.explain","title":"<code>explain(context)</code>","text":"<p>Genera una explicaci\u00f3n en lenguaje natural basada en un contexto dado utilizando un modelo de OpenAI.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>str</code> <p>Texto base para generar la explicaci\u00f3n.</p> required <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Respuesta generada por el modelo.</p> En caso de error <p>str: Mensaje de error.</p> Source code in <code>services\\llm.py</code> <pre><code>def explain(context: str) -&gt; str:\n    \"\"\"\n    Genera una explicaci\u00f3n en lenguaje natural basada en un contexto dado\n    utilizando un modelo de OpenAI.\n\n    Args:\n        context (str): Texto base para generar la explicaci\u00f3n.\n\n    Returns:\n        str: Respuesta generada por el modelo.\n\n    En caso de error:\n        str: Mensaje de error.\n    \"\"\"\n    try:\n        r = client.responses.create(\n            model=\"gpt-4.1-mini\",\n            temperature=0.2,\n            max_output_tokens=300,\n            input=[\n                {\"role\": \"system\", \"content\": \"Eres Loki, asistente CRM.\"},\n                {\"role\": \"user\", \"content\": context}\n            ]\n        )\n\n        return r.output_text\n    except Exception as e:\n        return f\"Error generando respuesta: {e}\"   \n</code></pre>"},{"location":"services/#crm-ghl","title":"CRM (GHL)","text":""},{"location":"services/#services.ghl.get_opportunities","title":"<code>get_opportunities()</code>  <code>async</code>","text":"<p>Obtiene la lista de oportunidades desde la API de GoHighLevel (GHL).</p> <p>Realiza una petici\u00f3n POST al endpoint de b\u00fasqueda de oportunidades.</p> <p>Returns:</p> Name Type Description <code>list</code> <p>Lista de oportunidades obtenidas desde el CRM.</p> <p>Raises:</p> Type Description <code>HTTPStatusError</code> <p>Si la API devuelve un error.</p> Source code in <code>services\\ghl.py</code> <pre><code>async def get_opportunities():\n    \"\"\"\n    Obtiene la lista de oportunidades desde la API de GoHighLevel (GHL).\n\n    Realiza una petici\u00f3n POST al endpoint de b\u00fasqueda de oportunidades.\n\n    Returns:\n        list: Lista de oportunidades obtenidas desde el CRM.\n\n    Raises:\n        httpx.HTTPStatusError: Si la API devuelve un error.\n    \"\"\"\n    # Para buscar oportunidades en la v2 con la versi\u00f3n 2021-04-15,\n    # se usa el m\u00e9todo POST y el locationId va en el cuerpo (JSON)\n    url = f\"{BASE_URL}/opportunities/search\"\n\n    payload = {\"locationId\": GHL_LOCATION_ID.strip()}\n\n    async with httpx.AsyncClient(timeout=15) as client:\n        # Enviamos la petici\u00f3n como POST con json=payload\n        r = await client.post(url, json=payload, headers=HEADERS)\n\n        if r.status_code != 200:\n            print(f\"Error de GHL detallado: {r.text}\")\n\n        r.raise_for_status()\n\n        # GHL devuelve un objeto que contiene una lista llamada 'opportunities'\n        data = r.json()\n        return data.get(\"opportunities\", [])\n</code></pre>"},{"location":"services/#services.ghl.move_opportunity_stage","title":"<code>move_opportunity_stage(opportunity_id, new_stage_id)</code>  <code>async</code>","text":"<p>Mueve una oportunidad a una nueva etapa dentro del pipeline.</p> <p>Parameters:</p> Name Type Description Default <code>opportunity_id</code> <code>str</code> <p>ID de la oportunidad.</p> required <code>new_stage_id</code> <code>str</code> <p>ID de la nueva etapa.</p> required <p>Returns:</p> Name Type Description <code>tuple</code> <ul> <li>int: C\u00f3digo de estado HTTP</li> <li>str: Respuesta de la API</li> </ul> Source code in <code>services\\ghl.py</code> <pre><code>async def move_opportunity_stage(opportunity_id, new_stage_id):\n    \"\"\"\n    Mueve una oportunidad a una nueva etapa dentro del pipeline.\n\n    Args:\n        opportunity_id (str): ID de la oportunidad.\n        new_stage_id (str): ID de la nueva etapa.\n\n    Returns:\n        tuple:\n            - int: C\u00f3digo de estado HTTP\n            - str: Respuesta de la API\n    \"\"\"\n    async with httpx.AsyncClient(timeout=15) as client:\n        r = await client.put(\n            f\"{BASE_URL}/opportunities/{opportunity_id}\",\n            json={\n                \"pipelineStageId\": new_stage_id\n            },\n            headers=HEADERS\n        )\n        return r.status_code, r.text\n</code></pre>"},{"location":"usage/","title":"Gu\u00eda de uso","text":""},{"location":"usage/#ejecutar-servidor","title":"\u25b6\ufe0f Ejecutar servidor","text":"<p>```bash uvicorn main:app --reload</p> <p>POST /loki/chat</p> <p>{   \"message\": \"mueve la oportunidad Juan a negociaci\u00f3n\" } Respuestas posibles {   \"intent\": {...},   \"found_opportunity\": {...},   \"resolved_stage_id\": \"...\" } {   \"answer\": \"Resumen diario CRM...\" }</p>"}]}